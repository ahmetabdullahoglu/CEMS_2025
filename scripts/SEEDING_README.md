# CEMS Database Seeding Guide

هذا الدليل يشرح كيفية ملء قاعدة البيانات ببيانات اختبار شاملة.

## 📋 متطلبات التشغيل

```bash
# تثبيت المكتبات المطلوبة
pip install faker==22.6.0

# أو تثبيت كل المتطلبات
pip install -r requirements.txt
```

## 🚀 طريقة الاستخدام السريعة

### الطريقة الأولى: كل شيء مرة واحدة (موصى بها)

```bash
# من داخل حاوية Docker
docker exec -it cems_app_dev bash

# ثم داخل الحاوية
python scripts/seed_data.py          # الأدوار والمستخدم الإداري
python scripts/seed_currencies.py   # العملات وأسعار الصرف
python scripts/seed_branches.py     # الفروع
python scripts/seed_comprehensive.py # كل البيانات المتبقية (موصى به!)
```

### الطريقة الثانية: البيانات الأساسية فقط

```bash
python scripts/seed_data.py
python scripts/seed_currencies.py
python scripts/seed_branches.py
python scripts/seed_vaults.py
python scripts/seed_customers.py
python scripts/seed_transactions.py
```

## 📊 البيانات المتوفرة

### 1. seed_data.py - الأدوار والمستخدم الإداري
- ✅ 3 أدوار: admin, manager, teller
- ✅ مستخدم admin بكلمة مرور: `Admin@123`

### 2. seed_currencies.py - العملات
- ✅ 8 عملات: USD, EUR, TRY, GBP, SAR, AED, JPY, EGP
- ✅ أسعار صرف بين كل العملات

### 3. seed_branches.py - الفروع
- ✅ 4-5 فروع في مناطق مختلفة
- ✅ أرصدة افتتاحية لكل فرع

### 4. seed_comprehensive.py - بيانات شاملة 🌟

**الوضع الكامل (الافتراضي):**
- 👥 **30+ مستخدم**
  - 2 admins
  - 10 managers
  - 18+ tellers
  - كلهم مع صلاحيات وتعيينات فروع

- 👤 **150+ عميل**
  - 85% أفراد، 15% شركات
  - كل عميل مع:
    - 1-3 وثائق (جواز سفر، هوية، إقامة...)
    - 0-3 ملاحظات
    - مستوى مخاطر واقعي
    - بيانات تحقق

- 🏦 **20+ خزنة**
  - 2-3 خزائن لكل فرع
  - أرصدة بعملات متعددة
  - أنواع مختلفة (رئيسية، احتياطية، نقد...)

- 💸 **50+ تحويل خزنة**
  - حالات متنوعة: مكتمل، قيد النقل، معلق، ملغي
  - بين خزائن وفروع مختلفة

- 💳 **750+ معاملة**
  - 60% صرافة (Exchange)
  - 20% تحويل (Transfer)
  - 10% إيرادات (Income)
  - 10% مصروفات (Expense)
  - بتواريخ موزعة على آخر 6 أشهر

**الوضع المصغر (للاختبار السريع):**
```bash
python scripts/seed_comprehensive.py --small
```
- 10 مستخدمين
- 30 عميل
- 60 معاملة
- 15 تحويل خزنة

## 🎯 السيناريوهات المدعومة

بعد تشغيل `seed_comprehensive.py`، يمكنك اختبار:

### ✅ إدارة المستخدمين
- تسجيل دخول بأدوار مختلفة (admin, manager, teller)
- عرض قائمة المستخدمين
- تعيين المستخدمين للفروع

### ✅ إدارة العملاء
- عرض قائمة العملاء مع pagination
- البحث والتصفية
- عرض تفاصيل العميل مع الوثائق والملاحظات
- سجل معاملات كل عميل

### ✅ المعاملات
- عرض معاملات الفرع
- تصفية حسب النوع، التاريخ، الحالة
- عرض تفاصيل كل نوع معاملة
- إحصائيات المعاملات

### ✅ إدارة الخزنة
- عرض أرصدة الخزائن
- سجل التحويلات
- الموافقة على التحويلات المعلقة
- تتبع التحويلات قيد النقل

### ✅ التقارير
- تقارير أداء الفروع
- تقارير المعاملات
- تقارير العملات
- تصدير البيانات

## 🔄 إعادة التعبئة

لحذف البيانات والبدء من جديد:

```bash
# حذف كل البيانات (احذر!)
python scripts/reset_database.py

# ثم أعد التعبئة
python scripts/seed_data.py
python scripts/seed_currencies.py
python scripts/seed_branches.py
python scripts/seed_comprehensive.py
```

## 💡 نصائح

1. **ابدأ بالترتيب الصحيح:**
   ```
   seed_data → seed_currencies → seed_branches → seed_comprehensive
   ```

2. **استخدم الوضع المصغر أولاً** للاختبار السريع:
   ```bash
   python scripts/seed_comprehensive.py --small
   ```

3. **تحديث الصلاحيات:** إذا قمت بتغيير الصلاحيات، شغّل:
   ```bash
   python scripts/seed_data.py  # سيحدث الأدوار تلقائياً
   ```

4. **مراقبة الذاكرة:** البيانات الشاملة قد تأخذ 2-5 دقائق حسب سرعة الجهاز

## 📝 بيانات الدخول

بعد التعبئة، يمكنك الدخول بـ:

**Admin:**
- Username: `admin`
- Password: `Admin@123`

**Managers:**
- Username: `manager01`, `manager02`, ... `manager10`
- Password: `Password@123`

**Tellers:**
- Username: `teller01`, `teller02`, ...
- Password: `Password@123`

## 🐛 استكشاف الأخطاء

### خطأ: "No roles found"
```bash
# شغّل seed_data أولاً
python scripts/seed_data.py
```

### خطأ: "No currencies found"
```bash
# شغّل seed_currencies أولاً
python scripts/seed_currencies.py
```

### خطأ: "ModuleNotFoundError: No module named 'faker'"
```bash
# ثبّت faker
pip install faker==22.6.0
```

### بطء في التنفيذ
```bash
# استخدم الوضع المصغر
python scripts/seed_comprehensive.py --small
```

## 📞 الدعم

للمساعدة أو الإبلاغ عن مشاكل، راجع:
- وثائق المشروع: `/docs`
- ملفات الـ seed: `/scripts`
